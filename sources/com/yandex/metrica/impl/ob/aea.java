package com.yandex.metrica.impl.ob;

import android.support.annotation.Nullable;
import com.yandex.metrica.Revenue;
import java.util.Arrays;
import java.util.List;
public class aea implements adw<Revenue> {
    private final adw<List<adu>> a = new adv();

    public adu a(@Nullable Revenue revenue) {
        return this.a.a(Arrays.asList(new adz().a(revenue.quantity)));
    }
}
