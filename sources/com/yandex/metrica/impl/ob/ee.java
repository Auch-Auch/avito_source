package com.yandex.metrica.impl.ob;

import android.support.annotation.NonNull;
public class ee {
    @NonNull
    public lc a(Thread thread) {
        return new lc(thread.getName(), thread.getPriority(), thread.getId(), ef.a(thread), null, null);
    }
}
