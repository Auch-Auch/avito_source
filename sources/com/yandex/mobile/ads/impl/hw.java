package com.yandex.mobile.ads.impl;

import android.support.annotation.NonNull;
import android.support.annotation.Nullable;
public interface hw<R, T> {
    hv a(@Nullable sa<T> saVar, int i, @NonNull R r);

    hv a(R r);
}
